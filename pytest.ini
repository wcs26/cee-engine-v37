[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    network: tests nécessitant un accès internet (API gouv, IGN, ADEME)
    ai: tests nécessitant une clé IA valide en env
timeout = 30
addopts = -ra --strict-markers
